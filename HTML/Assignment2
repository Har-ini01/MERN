1. What is the Shortcut Emmet Used to Create boilerplate of HTML ? !/html:5
2. What is DOCTYPE in Html ? declares the HTML version 
3. What is Void Element and Example for Void Elements ? Void elements are self-closing elements that don’t have closing tags Examples: <img>, <br>, <input>, <hr>.
4. What is Different Element and Attributes ? Elements are HTML components like <a>, while attributes provide additional information, such as href.
5. What is Html Entries and Why is it need in HTML ? HTML entities represent special characters.
6. What are meta tag and why is it used - They help with search engine ranking 
7. what is the best way to add images in Website ?  CDN
